services:
  - type: web
    name: object-detection-app
    env: python
    region: oregon
    rootDir: hf_spaces
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
